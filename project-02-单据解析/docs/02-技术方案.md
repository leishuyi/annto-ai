# 技术方案

## 解析管线架构

输入(拍照/扫描)
    ↓
文档分类器(LLM/关键词判断5大类型)
    ↓
图像预处理(增强/矫正)
    ↓
OCR文字提取(PaddleOCR)
    ↓
字段提取(按文档类型的结构化抽取)
    ↓
逻辑校验(跨字段一致性验证)
    ↓
输出(结构化JSON + 置信度标记)

## 置信度分级策略

| 级别 | 阈值 | 处理方式 |
|------|------|----------|
| 高 | >95% | 自动写入系统 |
| 中 | 80-95% | 人工抽检 |
| 低 | <80% | 强制人工录入 |
