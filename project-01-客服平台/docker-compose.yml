services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: starshield
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-starshield}
      POSTGRES_DB: starshield
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U starshield"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      STARSHIELD_DATABASE_URL: ${STARSHIELD_DATABASE_URL:-postgresql://starshield:starshield@db:5432/starshield}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules

volumes:
  pgdata:
