# 测试数据与调试指南

## 话术测试场景

| 场景 | 客户输入 | 预期回复风格 |
|------|---------|-------------|
| 投诉 | 你们物流太差了，我要投诉！ | 安抚+承诺跟进 |
| 延迟 | 说好上午到的怎么还没来？ | 解释+预计时间 |
| 破损 | 收到的时候箱子都破了 | 道歉+指导理赔 |
| 退货 | 这个我不要了退回去 | 确认退货流程 |
| 安装 | 你们管安装吗？ | 确认含安装服务 |

## 对账预期结果

输入周期 2026-07：
- 总发票：150 张
- 匹配：145 张
- 差异：5 张（差异示例：INV-2024-095 预期6500实收0）
- 差异原因：保险费未体现在发票中

## 预警预期

| ID | 类型 | 级别 | 预期根因 |
|----|------|------|---------|
| 1 | delivery_delay | warning | 浦东修路绕行 |
| 2 | inventory | critical | 销量超预期+工厂延迟 |
| 3 | damage | info | 正常范围 |

## 调试命令

```bash
cd backend && uvicorn app.main:app --reload --port 8012

# 测试导航
python -c "from app.driver.navigation import get_route; from app.schemas.driver import RouteRequest; r=get_route(RouteRequest(origin='A',destination='B')); print(f'{r.total_km}km')"

# 测试 RPA
python -c "from app.operations.rpa_agent import RPAFinanceAgent; r=RPAFinanceAgent(); print(r.reconcile('2026-07').model_dump())"

# 测试预警
python -c "from app.operations.alert_dashboard import get_alerts; print(get_alerts().model_dump())"
```
